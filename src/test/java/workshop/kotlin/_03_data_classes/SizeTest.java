package workshop.kotlin._03_data_classes;

import org.assertj.core.api.Assertions;

import static org.assertj.core.api.Assertions.*;

public class SizeTest {

    public void shouldProperlyCreateSizeDataClass() {


        fail("Create the data class `Size` in the Kotlin package so that uncommmenting the lines below will compile.");

//        Size result = new Size(3, 4);

//        assertThat(result.copy(3, 4)).isEqualTo(result);
//        assertThat(result.component1()).isEqualTo(3);
//        assertThat(result.component2()).isEqualTo(4);
//        assertThat(result.hashCode()).isNotNull();
    }
}
